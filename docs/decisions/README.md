# 决策记录制度（ADR）

W9-12 制度项。每个"不可轻易逆转"或"影响后续一切"的决策，落一条 ADR。
编号 D001 起，状态：accepted（在效）/ superseded（被取代，指向新 ADR）/ rejected（否决）。

**格式**：背景（当时信息）→ 决策（选了什么）→ 备选项（否决了什么及原因）→ 后果（可预见的影响）。

**触发线**：底座/架构/协议/数据口径/模型选择/安全边界——一句话，别人问"为什么这个项目长这样"，答案应该全部能从 ADR 里查到。

模板见 `_template.md`。
