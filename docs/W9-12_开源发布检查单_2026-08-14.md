# 开源发布检查单（2026-08-14）

> 状态：全部准备项 ✅ 完成，**push 动作待雅宁拍板**（公开/私有/暂缓）。
> push 是不可逆外向动作（公开 repo 被 fork/索引后撤回无意义），形态决定权在用户。

## ✅ 已完成的准备项

| # | 项 | 证据 |
|---|---|---|
| 1 | 零凭据扫描 | vendor 外全仓 grep `sk-[a-zA-Z0-9]{20,}` 零命中 |
| 2 | config.yaml 零 token | 只有 model/plugins/memory 配置 |
| 3 | git 历史零泄露 | `git log --all -p | grep sk-40fb` = 0 |
| 4 | HANDOFF gitignored | `.gitignore` 首条注释写明，从未入库 |
| 5 | 运行时数据 gitignored | state.db/memories/auth.json/qra_*.db/bench/isolation/ 全覆盖 |
| 6 | kb_sources 脱敏 | 14 处人称替换（7 文件），两 KB 重灌一致（FTS 687 行探针 0 失败 + Chroma 687 块） |
| 7 | README 开源级重写 | 能力表/快速开始/评测结果/架构/安全声明 |
| 8 | LICENSE | MIT + vendor 不属发布物声明 |
| 9 | 决策记录可公开 | docs/decisions/ D001-D006（无敏感信息） |
| 10 | vendor 处理策略 | gitignored + README 写明 pin 17d6a7d 获取方式；发布物不含上游副本 |

## ⏸ 待雅宁拍板

1. **形态**：公开 GitHub / 私有 GitHub / 暂缓 push（本机 repo 即交付）
2. **账号与仓库名**：用哪个 GitHub 账号、repo 名（若公开，账号名=身份披露面）
3. **LICENSE 确认**：MIT 默认待确认
4. **vendor 发布形态**（若公开）：submodule 指针 / README 引用声明（当前做法）/ 打包时排除

## push 后首周义务（W9-12 计划）

- 成本周检连续执行（scripts/check_cost.py，阈值 ¥350/周）
- 明文 key `sk-40fb…` 轮换（用户动作，HANDOFF 中标注）
- bench 难题层设计（若需要能力对比）
